#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        # BeforePaste state is per-user. Package removal must not mutate a
        # user's config directory or shortcuts from a root maintainer script.
        ;;
esac
